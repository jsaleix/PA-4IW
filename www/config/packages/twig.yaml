twig:
    default_path: '%kernel.project_dir%/templates'
    paths:
        '%kernel.project_dir%/templates/front': front
        '%kernel.project_dir%/templates/back': back
        

when@test:
    twig:
        strict_variables: true
