{% extends 'base_back.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ users.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ users.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ users.roles ? users.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ users.password }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ users.city }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ users.address }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ users.phone }}</td>
            </tr>
            <tr>
                <th>StripeConnectId</th>
                <td>{{ users.stripeConnectId }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ users.name }}</td>
            </tr>
            <tr>
                <th>Surname</th>
                <td>{{ users.surname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_user_dashboard') }}">back to list</a>

    <a href="{{ path('admin_user_edit', {'id': users.id}) }}">edit</a>

    {{ include('back/admin_user/_delete_form.html.twig') }}
{% endblock %}
