<form method="post" action="{{ path('admin_user_delete', {'id': users.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ users.id) }}">
    <button class="btn">Delete</button>
</form>
