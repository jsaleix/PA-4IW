<!DOCTYPE html>

<title>Edit User</title>

{% block body %}
    <h1>Edit User</h1>

    {{ include('back/admin_user/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_user_index') }}">back to list</a>

    {{ include('back/admin_user/_delete_form.html.twig') }}
{% endblock %}
