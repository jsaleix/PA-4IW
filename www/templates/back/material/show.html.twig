<!DOCTYPE html>

<title>Material</title>

{% block body %}
    <h1>Material</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ material.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ material.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ material.description }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ material.icon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('material_index') }}">back to list</a>

    <a href="{{ path('material_edit', {'id': material.id}) }}">edit</a>

    {{ include('back/material/_delete_form.html.twig') }}
{% endblock %}
