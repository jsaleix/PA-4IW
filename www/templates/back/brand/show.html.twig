{% extends 'base_back.html.twig' %}

{% block title %}Brand{% endblock %}


{% block body %}
    <h1>Brand</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brand.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ brand.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ brand.description }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ brand.logo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_brand_index') }}">back to list</a>

    <a href="{{ path('back_brand_edit', {'id': brand.id}) }}">edit</a>

    {{ include('back/brand/_delete_form.html.twig') }}
{% endblock %}
