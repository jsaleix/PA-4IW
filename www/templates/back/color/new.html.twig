{% extends 'base_back.html.twig' %}

{% block title %}New Color{% endblock %}

{% block body %}
    <h1>Create new Color</h1>

    {{ form_start(form) }}
        {{ form_rest(form) }}
        <button type="submit">Créer</button>
    {{ form_end(form) }}

    <a href="{{ path('color_index') }}">back to list</a>
{% endblock %}
