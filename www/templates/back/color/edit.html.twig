{% extends 'base_back.html.twig' %}

{% block title %}Edit Color{% endblock %}

{% block body %}
    <h1>Edit Color</h1>

        {{ form_start(form) }}
            {{ form_rest(form) }}
            <button type="submit">Appliquer</button>
        {{ form_end(form) }}

    <a href="{{ path('color_index') }}">back to list</a>

    {{ include('back/color/_delete_form.html.twig') }}
{% endblock %}
