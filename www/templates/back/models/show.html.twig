{% extends 'base_back.html.twig' %}
{% block title %}SNKER - Models {% endblock %}

{% block body %}
    <h1>SneakerModel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sneaker_model.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sneaker_model.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ sneaker_model.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_sneaker_model_index') }}">back to list</a>

    <a href="{{ path('back_sneaker_model_edit', {'id': sneaker_model.id}) }}">edit</a>

    {{ include('back/models/_delete_form.html.twig') }}
{% endblock %}
