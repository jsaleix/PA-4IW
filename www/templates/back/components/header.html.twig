<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{path('front_default') }}">
        <img id="logo" src="{{asset('images/logo.png')}}" alt="SNKERS logo"/>
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_dashboard_index') }}">Dashboard</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_shop_index') }}">Shop</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_marketplace_index') }}">Marketplace</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_user_dashboard') }}">Users</a>
            </li>
            {% if ( app.user ) %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        {{app.user.username}}
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                        <a class="dropdown-item" href="{{ path('default') }}">Front</a>
                        <a class="dropdown-item" href="/account">Account</a>
                        <a class="dropdown-item" href="/logout">Logout</a>
                    </div>
                </li>
            {% else %}
                <li>
                    <a href="/login" class="btn btn-outline-success my-2 my-sm-0" type="submit">Login</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>


<style>
    #logo{
        width: 150px;
    }

    nav{
        box-sizing: border-box;
        width: 100%;
        height: 100px;
        box-sizing: border-box;
        display: flex;
        flex-direction: row;
        justify-content: space-between;
        padding: 0 20%;
        align-items: center;
        border-bottom: 1px solid black;
    }
</style>
